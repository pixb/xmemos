import 'package:flutter/material.dart';

class MonitorXPage extends StatelessWidget {
  const MonitorXPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('MonitorX'),
      ),
      body: const Center(
        child: Text('Welcome to MonitorX!'),
      ),
    );
  }
}